import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  icon: {
    fontSize: "2em",
  },
  button: {
    color: "#1F2833",
  },
  container: {
    display: "flex",
    justifyContent: "center",
    marginBottom: "2em",
    marginRight: "8em",
  },
}));
